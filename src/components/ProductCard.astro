---
interface Props {
  title: string;
  h1: string;
  slug: string;
  price: number;
  discount?: {
    percentage: number;
  };
  rating: number;
  shortDescription: string;
  affiliateUrl: string;
}

const { title, h1, slug, rating, shortDescription, affiliateUrl } = Astro.props;

const stars = Array.from({ length: 5 }, (_, i) => i < Math.floor(rating));

// Extraer el DP del enlace de Amazon
const getDPFromAmazonUrl = (url: string) => {
  const match = url.match(/\/([A-Z0-9]{10})(?:\/|\?|$)/);
  return match ? match[1] : '';
};

const dp = getDPFromAmazonUrl(affiliateUrl);
---

<article class="group bg-white rounded-2xl overflow-hidden shadow-card hover:shadow-hover transition-all duration-300">
  <figure class="aspect-w-16 aspect-h-9 bg-gray-100 relative">
    <img
      src={`/imagenes/${slug}.jpg`}
      alt={h1}
      class="w-full h-full object-cover object-center"
    />
  </figure>

  <div class="p-6">
    <header class="mb-3">
      <div class="flex items-center gap-1.5">
        {stars.map((filled) => (
          <svg
            class={`w-5 h-5 ${filled ? 'text-yellow-500' : 'text-gray-200'}`}
            fill="currentColor"
            viewBox="0 0 20 20"
            aria-hidden="true"
          >
            <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
          </svg>
        ))}
        <span class="text-sm font-medium text-gray-700 ml-1" aria-label={`${rating.toFixed(1)} out of 5 stars`}>
          {rating.toFixed(1)}
        </span>
      </div>

      <h2 class="text-xl font-bold text-gray-900 group-hover:text-primary-600 transition-colors line-clamp-2 mt-2">
        <a href={`/products/${slug}/`} class="focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 rounded-lg">
          {h1}
        </a>
      </h2>
    </header>

    <p class="text-base text-gray-600 line-clamp-2 mb-4">{shortDescription}</p>
  </div>
</article>